import React from 'react';
import EmptyScreen from '@components/EmptyScreen';

const JobDetailScreen: React.FC = () => {
  return <EmptyScreen screenName={'Job Detail'} />;
};

export default JobDetailScreen;
